# NanoGPT push record

Same as `Nanogpt base` experiment, except we will be using every technique to pass the record

2 runs will be performed, a run on a local machine will be done (with gradient accumulation), then, we will do a run on a H100 cluster (with fp8 training enabled)

# Results
