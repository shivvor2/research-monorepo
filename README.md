# Research Monorepo

## What is this
This is a monorepo containing multiple model training experiements

## List of experiements

(TBD, link to Project README)

## Installation

Please install the project in editable mode (with dev dependencies), Ideally in a managed environment

```bash
pip install -e .[dev]
```

Then, install precommit hooks
```
pre-commit install
```
